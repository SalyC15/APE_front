# APE_front

Frontend Flutter de MediCity para el sistema médico distribuido.

## Ejecutar

```powershell
flutter pub get
flutter run
```

La aplicación consume la API .NET configurada en `lib/services/api_service.dart`.
